#include <stdio.h>
int main(void){
    int a,b,c;
    float avg;
    //Type casting
    //int/int = int, make any one float
    printf("Enter three numbersn\n");
    scanf("%d %d %d", &a, &b, &c);
    avg = (a + b + c)/3.0;     //Implicit Typescasting
    avg = (float) (a + b + c)/3; //Explicit

    //HW questions
    //Enter 5 subject marks then calculate total and percentage
    //calculate are of circle
    //calculate area of triangle
    /*enter the basic salary of an employ
     DA = 12.55%
     TA = 17.25%
     HRA = 12.50%
     PF = 13.23%
     LIC = 17.25% */
    // F to C converter
    // C to F converter
    // Enter two number than swap with extra variable
    // without variable
    // without arthmatical operator
    // Create a BMI calculator
}
